// this file is the program's Entry Point
import { buttonClick } from "../handlers/handler.js"

const buttonColor = document.getElementById('set-color');


buttonColor.addEventListener('click', buttonClick);